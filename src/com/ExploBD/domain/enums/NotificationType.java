package com.ExploBD.domain.enums;

public enum NotificationType {
    GROUP_INVITATION,
    FRIEND_REQUEST,
    INVITATION_APPROVED,
    MEMBER_JOINED,
    VOTE_STARTED,
    DESTINATION_CONFIRMED,
    GROUP_CREATED,
    EXPENSE_ADDED,
    SETTLEMENT_REMINDER,
    NEW_SUGGESTION,
    JOIN_REQUEST,
    FRIEND_REQUEST_ACCEPTED, NEW_MESSAGE, CHECKLIST_ADDED      // Add this for when friend request is accepted
}
